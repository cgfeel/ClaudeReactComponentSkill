# ClaudeReactComponentSkill
自定义一个 React Component Generator，使用的技术栈有：React、TypeScript、Tailwind CSS

> 本仓库只为学习目的，如需要可自行调整 `skill`

## 提示词举例

请按照 `React` 组件生成器 `skill` 的规范，创建一个 `BookmarkCard` 组件

- 组件功能：展示单个书签的卡片，显示标题、`URL`、描述和标签列表
- 需要 `props` 不需要状态管理
